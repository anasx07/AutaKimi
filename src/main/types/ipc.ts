import { IpcChannel, ElectronApi } from '../../common/types/ipc';

export { IpcChannel };
export type { ElectronApi };

/**
 * Any additional main-specific types can go here, 
 * but core IPC definitions should stay in common/types/ipc.ts
 */
